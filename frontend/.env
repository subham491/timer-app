# Backend API base path. Same-origin by design: the Vite dev proxy
# (vite.config.ts) routes /api to the backend; in production the reverse
# proxy does the same. Never an absolute URL — the login redirect and
# cookies both depend on staying on one origin.
VITE_API_BASE_URL=/api